// This file contains constants for sorting step types
// In JavaScript, we don't need type definitions, but we keep this for documentation

/**
 * Possible types for a sorting step:
 * - 'comparison': Comparing two elements
 * - 'swap': Swapping two elements
 * - 'sorted': Marking elements as sorted
 */

// Example of a sorting step object:
// {
//   type: 'comparison' | 'swap' | 'sorted',
//   indices: number[],
//   array: number[]
// } 